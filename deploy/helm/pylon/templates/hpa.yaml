{{- if .Values.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "pylon.fullname" . }}
  labels:
    {{- include "pylon.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "pylon.fullname" . }}
  minReplicas: {{ include "pylon.plainNumber" .Values.autoscaling.minReplicas }}
  maxReplicas: {{ include "pylon.plainNumber" .Values.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ include "pylon.plainNumber" .Values.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ include "pylon.plainNumber" .Values.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
