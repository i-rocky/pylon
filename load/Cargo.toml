[package]
name = "pylon-load"
version = "0.1.0"
edition = "2021"
description = "Pusher-protocol load-test harness for pylon"
publish = false

[lib]
name = "pylon_load"
path = "src/lib.rs"

[[bin]]
name = "pylon-load"
path = "src/main.rs"

[[bin]]
name = "pylon-ceiling"
path = "src/bin/ceiling.rs"

[dependencies]
tokio = { version = "1", features = ["rt-multi-thread", "macros", "net", "sync", "time", "signal", "process"] }
tokio-tungstenite = "0.30"
url = "2"
futures-util = "0.3"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
# Plain `rustls` (not the server's `rustls-no-provider`): this harness installs
# no process-global crypto provider of its own.
reqwest = { version = "0.13", default-features = false, features = ["rustls"] }
hmac = "0.13"
sha2 = "0.11"
md-5 = "0.11"
hex = "0.4"
hdrhistogram = "7"
clap = { version = "4", features = ["derive"] }
anyhow = "1"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

[dev-dependencies]
# Ensures the pylon crate builds alongside these tests. The child-spawning tests
# (smoke, ceiling_*) exec the separately-built `target/<profile>/pylon` binary.
pylon = { path = ".." }
